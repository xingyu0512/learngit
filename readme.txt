/*关于git：
准备好你需要用git来push的文件，并放置在计算机本地库位置，如：C：/ users / X / learninggit。*/
git基本指令：
{
1.上传文件：
git add x.txt(文件名) --将x.txt文件加入到库里

git commit -m "XXX"--对需要提交的文件作备注，备注为：XXX

git push origin master--上传这批文件
}